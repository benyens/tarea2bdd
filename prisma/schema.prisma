// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
 
model usuarios {
  id Int @id @default(autoincrement())
  direccion_correo String @unique
  nombre_usuario String
  contrasena String
  descripcion String
  bloqueado Boolean @default(false)
  fecha_creacion DateTime @default(now())
  correosEnviados correos[] @relation(name: "CorreoRemitente")
  correosRecibidos correos[] @relation(name: "CorreoDestinatario")
  direcciones_favoritas direcciones_favoritas[]
  direcciones_bloqueadas direcciones_bloqueadas[]
}

model correos {
  id Int @id @default(autoincrement())
  remitenteId Int // clave foránea
  destinatarioId Int // clave foránea
  fecha_envio DateTime @default(now())

  remitente usuarios @relation(name: "CorreoRemitente", fields: [remitenteId], references: [id])
  destinatario usuarios @relation(name: "CorreoDestinatario", fields: [destinatarioId], references: [id])
}

model direcciones_favoritas {
  id Int @id @default(autoincrement())
  usuarioId Int // clave foránea
  direccion_favorita String

  usuario usuarios @relation(fields: [usuarioId], references: [id])
}

model direcciones_bloqueadas {
  id Int @id @default(autoincrement())
  usuarioId Int // clave foránea
  direccion_bloqueada String

  usuario usuarios @relation(fields: [usuarioId], references: [id])
}
 
